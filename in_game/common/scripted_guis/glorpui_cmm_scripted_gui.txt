#noinspection ALL
# Toggles (toggles) Tab
## Left Panel (leftpanel) Group
glorpui__useFramedStaticFlag_on_changed = {
	scope = country

	is_shown = {
		has_variable = useStaticFlag
	}
	effect = { }
}

# Build Panel (build_panel) Tab
## General (general) Group
glorpui__disableRoiIndirect_on_changed = {
	scope = country

	is_shown = {
		is_target_in_global_variable_list = {
			name = cmf_active_mod_ids
			target = flag:cm
		}
	}
	effect = { }
}
