#noinspection ALL
# Toggles (toggles) Tab
## Left Panel (leftpanel) Group
glorpui__useFramedStaticFlag_on_changed = {
	scope = country

	is_shown = {
		has_variable = useStaticFlag
	}
	effect = { }
}
