# True when the Construction Manager mod is active.
glorpui_is_cm_active = {
	scope = country

	is_shown = {
		has_global_variable_list = cmf_active_mod_ids
		is_target_in_global_variable_list = {
			name = cmf_active_mod_ids
			target = flag:cm
		}
	}
	effect = { }
}
