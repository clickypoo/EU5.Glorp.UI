# Suppresses used-but-never-set warnings for the GUI-set RGO cost and unit icon scopes.
glorpui_gui_scope_suppress = {
	if = {
		limit = { always = no }
		save_scope_as = cm_location
		save_scope_as = cm_country
		save_scope_as = icon_idx
	}
}
