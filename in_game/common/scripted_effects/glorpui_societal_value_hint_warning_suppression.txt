# Suppresses used-but-never-set warnings for the GUI-set societal value hint scopes.
glorpui_svh_suppress = {
	if = {
		limit = { always = no }
		save_scope_as = glorpui_sv
		save_scope_as = glorpui_country
	}
}
