#noinspection ALL
# Root scope: country.
glorpui_register_cmf_mod = {
	# Toggles (toggles) Tab
	## General (general) Group
	cmm_register_bool_setting = {
		mod_id = glorpui
		setting_id = useVanillaPortrait
		tab_id = toggles
		group_id = general
		default_value = 0
	}
	cmm_sync_bool_alias = {
		setting = glorpui__useVanillaPortrait
		alias = useVanillaPortrait
	}

	cmm_register_bool_setting = {
		mod_id = glorpui
		setting_id = useStaticFlag
		tab_id = toggles
		group_id = general
		default_value = 0
	}
	cmm_sync_bool_alias = {
		setting = glorpui__useStaticFlag
		alias = useStaticFlag
	}

	cmm_register_bool_setting = {
		mod_id = glorpui
		setting_id = useDefaultIconsBGLeftPanel
		tab_id = toggles
		group_id = general
		default_value = 0
	}
	cmm_sync_bool_alias = {
		setting = glorpui__useDefaultIconsBGLeftPanel
		alias = useDefaultIconsBGLeftPanel
	}

	cmm_register_bool_setting = {
		mod_id = glorpui
		setting_id = useDefaultIconsLeftPanel
		tab_id = toggles
		group_id = general
		default_value = 0
	}
	cmm_sync_bool_alias = {
		setting = glorpui__useDefaultIconsLeftPanel
		alias = useDefaultIconsLeftPanel
	}

	cmm_register_bool_setting = {
		mod_id = glorpui
		setting_id = useVanillaCabinetView
		tab_id = toggles
		group_id = general
		default_value = 0
	}
	cmm_sync_bool_alias = {
		setting = glorpui__useVanillaCabinetView
		alias = useVanillaCabinetView
	}

	cmm_register_bool_setting = {
		mod_id = glorpui
		setting_id = disableLeftPanelStatsBar
		tab_id = toggles
		group_id = general
		default_value = 1
	}
	cmm_sync_bool_alias_inverted = {
		setting = glorpui__disableLeftPanelStatsBar
		alias = disableLeftPanelStatsBar
	}

	cmm_register_bool_setting = {
		mod_id = glorpui
		setting_id = disableColouredMapmodeButtons
		tab_id = toggles
		group_id = general
		default_value = 1
	}
	cmm_sync_bool_alias_inverted = {
		setting = glorpui__disableColouredMapmodeButtons
		alias = disableColouredMapmodeButtons
	}

	# War View (war_view) Tab
	## General (general) Group
	cmm_register_bool_setting = {
		mod_id = glorpui
		setting_id = useVanillaArmyStrength
		tab_id = war_view
		group_id = general
		default_value = 0
	}
	cmm_sync_bool_alias = {
		setting = glorpui__useVanillaArmyStrength
		alias = useVanillaArmyStrength
	}

	cmm_register_bool_setting = {
		mod_id = glorpui
		setting_id = useVanillaParticipantColour
		tab_id = war_view
		group_id = general
		default_value = 0
	}
	cmm_sync_bool_alias = {
		setting = glorpui__useVanillaParticipantColour
		alias = useVanillaParticipantColour
	}

	# Top Bar (top_bar) Tab
	## General (general) Group
	cmm_register_bool_setting = {
		mod_id = glorpui
		setting_id = disableTopBarIncome
		tab_id = top_bar
		group_id = general
		default_value = 1
	}
	cmm_sync_bool_alias_inverted = {
		setting = glorpui__disableTopBarIncome
		alias = disableTopBarIncome
	}

	cmm_register_bool_setting = {
		mod_id = glorpui
		setting_id = disableTopBarStability
		tab_id = top_bar
		group_id = general
		default_value = 1
	}
	cmm_sync_bool_alias_inverted = {
		setting = glorpui__disableTopBarStability
		alias = disableTopBarStability
	}

	cmm_register_bool_setting = {
		mod_id = glorpui
		setting_id = disableTopBarPopulation
		tab_id = top_bar
		group_id = general
		default_value = 1
	}
	cmm_sync_bool_alias_inverted = {
		setting = glorpui__disableTopBarPopulation
		alias = disableTopBarPopulation
	}

	cmm_register_bool_setting = {
		mod_id = glorpui
		setting_id = disableTopBarLegitimacy
		tab_id = top_bar
		group_id = general
		default_value = 1
	}
	cmm_sync_bool_alias_inverted = {
		setting = glorpui__disableTopBarLegitimacy
		alias = disableTopBarLegitimacy
	}

	cmm_register_bool_setting = {
		mod_id = glorpui
		setting_id = disableTopBarPrestige
		tab_id = top_bar
		group_id = general
		default_value = 1
	}
	cmm_sync_bool_alias_inverted = {
		setting = glorpui__disableTopBarPrestige
		alias = disableTopBarPrestige
	}

	cmm_register_bool_setting = {
		mod_id = glorpui
		setting_id = disableTopBarDiplomats
		tab_id = top_bar
		group_id = general
		default_value = 1
	}
	cmm_sync_bool_alias_inverted = {
		setting = glorpui__disableTopBarDiplomats
		alias = disableTopBarDiplomats
	}

	cmm_register_bool_setting = {
		mod_id = glorpui
		setting_id = dynamicWarStats
		tab_id = top_bar
		group_id = general
		default_value = 0
	}
	cmm_sync_bool_alias = {
		setting = glorpui__dynamicWarStats
		alias = dynamicWarStats
	}
}

# Callback handler for cmf_on_callback.
# var:cmf_callback is the flag of the setting, alert, or action bar element that was interacted with.
# Scope: country
glorpui_handle_cmf_callback = {
	switch = {
		trigger = var:cmf_callback
		# Toggles (toggles) Tab
		## General (general) Group
		flag:glorpui__useVanillaPortrait = {
			cmm_sync_bool_alias = {
				setting = glorpui__useVanillaPortrait
				alias = useVanillaPortrait
			}
		}
		flag:glorpui__useStaticFlag = {
			cmm_sync_bool_alias = {
				setting = glorpui__useStaticFlag
				alias = useStaticFlag
			}
		}
		flag:glorpui__useDefaultIconsBGLeftPanel = {
			cmm_sync_bool_alias = {
				setting = glorpui__useDefaultIconsBGLeftPanel
				alias = useDefaultIconsBGLeftPanel
			}
		}
		flag:glorpui__useDefaultIconsLeftPanel = {
			cmm_sync_bool_alias = {
				setting = glorpui__useDefaultIconsLeftPanel
				alias = useDefaultIconsLeftPanel
			}
		}
		flag:glorpui__useVanillaCabinetView = {
			cmm_sync_bool_alias = {
				setting = glorpui__useVanillaCabinetView
				alias = useVanillaCabinetView
			}
		}
		flag:glorpui__disableLeftPanelStatsBar = {
			cmm_sync_bool_alias_inverted = {
				setting = glorpui__disableLeftPanelStatsBar
				alias = disableLeftPanelStatsBar
			}
		}
		flag:glorpui__disableColouredMapmodeButtons = {
			cmm_sync_bool_alias_inverted = {
				setting = glorpui__disableColouredMapmodeButtons
				alias = disableColouredMapmodeButtons
			}
		}
		# War View (war_view) Tab
		## General (general) Group
		flag:glorpui__useVanillaArmyStrength = {
			cmm_sync_bool_alias = {
				setting = glorpui__useVanillaArmyStrength
				alias = useVanillaArmyStrength
			}
		}
		flag:glorpui__useVanillaParticipantColour = {
			cmm_sync_bool_alias = {
				setting = glorpui__useVanillaParticipantColour
				alias = useVanillaParticipantColour
			}
		}
		# Top Bar (top_bar) Tab
		## General (general) Group
		flag:glorpui__disableTopBarIncome = {
			cmm_sync_bool_alias_inverted = {
				setting = glorpui__disableTopBarIncome
				alias = disableTopBarIncome
			}
		}
		flag:glorpui__disableTopBarStability = {
			cmm_sync_bool_alias_inverted = {
				setting = glorpui__disableTopBarStability
				alias = disableTopBarStability
			}
		}
		flag:glorpui__disableTopBarPopulation = {
			cmm_sync_bool_alias_inverted = {
				setting = glorpui__disableTopBarPopulation
				alias = disableTopBarPopulation
			}
		}
		flag:glorpui__disableTopBarLegitimacy = {
			cmm_sync_bool_alias_inverted = {
				setting = glorpui__disableTopBarLegitimacy
				alias = disableTopBarLegitimacy
			}
		}
		flag:glorpui__disableTopBarPrestige = {
			cmm_sync_bool_alias_inverted = {
				setting = glorpui__disableTopBarPrestige
				alias = disableTopBarPrestige
			}
		}
		flag:glorpui__disableTopBarDiplomats = {
			cmm_sync_bool_alias_inverted = {
				setting = glorpui__disableTopBarDiplomats
				alias = disableTopBarDiplomats
			}
		}
		flag:glorpui__dynamicWarStats = {
			cmm_sync_bool_alias = {
				setting = glorpui__dynamicWarStats
				alias = dynamicWarStats
			}
		}
	}
}
