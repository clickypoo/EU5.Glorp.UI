template AverageControlTooltip {
	ContextualTooltipType = {
		blockoverride "title_icon" {
			icon = {
				using = tooltip_title_icon_size
				texture = "[GetConceptTexture('control')]"
			}
		}

		blockoverride "title_text" {
			text = "AVERAGE_CONTROL_TT_TITLE"
		}

		blockoverride "title_button" {
			mapmode_tooltip_button = {
				datacontext = "[GetMapMode('control')]"
			}
		}

		blockoverride "concept_link" {
			text = [control|E]
		}

		blockoverride "tooltip_content" {
			TooltipStringPairList = {
				blockoverride "block_title" {
					text = "AVERAGE_CONTROL_TT_TABLE"
				}
				textcontext = "[Country.GetAverageControlInfo]"
			}
		}
	}
}
